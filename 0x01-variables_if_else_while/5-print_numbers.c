#include <stdio.h>
/**
  * main - Entry Point
  * Description: prints all single digit numbers
  * of base 10 starting from 0, followed by a new line
  * Return: 0
  */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d\n", i);
	}
	return (0);
}
